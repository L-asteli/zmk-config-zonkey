CONFIG_ZMK_KEYBOARD_NAME="zonkey_dongle"

# USB設定（ホストとしてPC/Macに接続）
CONFIG_ZMK_USB=y

# BLE設定（ペリフェラル機器と通信）
CONFIG_ZMK_BLE=y

# キーボードとして入力デバイスを持たないためスプリット機能は不要
# CONFIG_ZMK_SPLIT_ROLE_CENTRAL は使用しない
